* pdf2txt: content is (cid:4193) (cid:3944)
* support multi format: chm, doc, text
* maintain book files in repo
* fix: pdf2txt may hang forever
* find similar/duplicated book by content
* setup ftp for uploading
* check duplicated files(if fingerprint is the same, need check content md5)
* Perl/perl will return different result
